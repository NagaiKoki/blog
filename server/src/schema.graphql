type Query {
  posts: [Post]
}

type Mutation {
  post(title: String!, content: String!): Post
}

type Post {
  id: ID!
  title: String!
  content: String!
  status: Int!
  createdAt: String!
  updatedAt: String!
}
